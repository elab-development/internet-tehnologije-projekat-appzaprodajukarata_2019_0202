import { InputBorderHighlightDirective } from './input-border-highlight.directive';

describe('InputBorderHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBorderHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
